# Cinematic foundation

WebGL scenes belong here and should be dynamically imported by the route or section that needs them. The initial public pass uses lightweight CSS placeholders so pages stay fast on mobile; `three/threshold-scene.tsx` is an isolated R3F starting point for a future threshold interaction.